# Variables de entorno para Vite (expuestas en el navegador)
# Copia este archivo a `.env` o `.env.local` y ajusta los valores.
# ADVERTENCIA: No pongas secretos reales aquí; cualquier valor será visible en el cliente.

# URL base de la API (prod/staging/dev)
VITE_API_BASE_URL=https://api.tu-dominio.com

# Endpoint para preguntas públicas
VITE_API_ENDPOINT_ASK=/ask_public

# Si tu API requiere una API Key pública (scope limitado) puedes incluirla aquí.
# Idealmente, usa un backend/proxy para firmar peticiones y no exponer claves sensibles.
# Nombre del header que espera tu API (por defecto x-api-key)
VITE_API_KEY_HEADER=x-api-key
# Valor de la API key pública (NO PONGAS SECRETOS DE SERVER AQUÍ)
VITE_API_KEY=
